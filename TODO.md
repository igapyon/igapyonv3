# TODO

- OGP: `og:image` の適切な画像を用意して `<meta property="og:image" ...>` を追加する。
- Twitter: `twitter:image` の適切な画像を用意して `<meta name="twitter:image" ...>` を追加する。
- canonical を `https://www.igapyon.jp/igapyon/diary/` を基準に設定する（生成時に baseurl を使う）。
